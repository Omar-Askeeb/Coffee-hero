/// Merge these routes into your existing `app_routes.dart`.
///
/// If you already have an `AppRoutes` class, just copy the missing fields.
class AppRoutes {
  static const String login = '/login';
  static const String register = '/register';
  static const String forgotPhone = '/forgot-phone';
  static const String resetPassword = '/reset-password';

  /// Update to match your project home route.
  static const String home = '/';
}
